[package]
name = "moonlight-ffi"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
moonlight-dsp = { path = "../moonlight-dsp" }
moonlight-model = { path = "../moonlight-model" }

[lib]
path = "src/lib.rs"
crate-type = ["staticlib", "cdylib", "rlib"]
